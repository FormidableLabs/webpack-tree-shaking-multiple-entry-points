export const red = function(id, msg) {
  return '<h1 id="' + id + '" style="color: red">' + msg + "</h1>";
};
export const blue = function(id, msg) {
  return '<h1 id="' + id + '" style="color: blue">' + msg + "</h1>";
};
